package jpabook.japshop.domain.전체적개념설명;

public class JoinColumn에대해 {

    /**
     * 외래키를 맵핑할 때 사용
     *
     * 1. name : 맵핑할 외래키 이름
     *
     * 2. referenceColumName 외래 키가 참조하는 대상 테이블의 컬럼명
     *      참조하는 대상 테이블의 컬럼명이 다를 경우
     *
     * 3. foreignKey(DDL) 외래키 제약조건 직접 지정 가능 테이블 생성시만 사용
     *
     * 4. @Column 속성들
     *
     */
}
